{
  "name": "home-network",
  "version": "1.0.0",
  "description": "Secure home server setup with Docker Compose and Traefik",
  "packageManager": "pnpm@8.15.4",
  "scripts": {
    "format": "prettier --write .",
    "prepare": "husky install",
    "lint:shell": "shellcheck scripts/*.sh",
    "lint": "pnpm run lint:shell"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "prettier": "^3.2.5",
    "shellcheck": "^3.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
